configuration:
  AZURE_BICEP_FILE_EXPANSION: true

output:
  culture: ['en-AU']

input:
  pathIgnore:
    - '*' # Exclude all files
    - '!modules/**/*.test.bicep' # But allow, test files
    - '!modules/**/*.bicepparam' # Finally, test bicep parameter files
